@AGENTS.md

<!-- This file exists only so Claude Code picks up AGENTS.md. Keep it at one import line:
     the substance belongs in AGENTS.md so every agent (Claude Code, Codex, Aider) and
     every human reads the same file, and there is only one line budget to enforce.
     If the import above did not resolve for you, open AGENTS.md now and read it before
     doing anything else -- the failure mode of this indirection is an agent that has no
     project context and does not know it. -->
